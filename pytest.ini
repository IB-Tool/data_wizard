[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
markers =
    unit: fast unit test - no real QGIS required
    integration: requires a full QGIS Processing environment (Docker)
    edge_case: boundary or degenerate input scenario
    performance: exercises time or memory bounds on larger datasets
    slow: excluded from fast local runs via -m "not slow"
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
